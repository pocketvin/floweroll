# 安全说明

花卷是本机个人执行 Agent，不是多租户托管服务。模型输出和观察内容都不构成执行授权；写操作依赖目标身份、用户许可、幂等记录与执行后核验。

## 部署

Host 默认只监听 loopback；真实手机通过用户控制的 HTTPS 入口和配对 token 访问。不得绕过认证直接开放到公网。API Key 留在 Host，本地配置设为仅所有者可读。

开发观察台可能包含完整 Prompt、联系人、日程、OCR、截图和模型响应。公开部署前必须关闭完整采集，不把开发者 API 误当成匿名可访问的产品接口。

## 报告问题

不要在公开 Issue 中上传 token、用户任务数据库、未脱敏日志、截图或个人设备信息。仓库正式上线后，优先启用 GitHub Private Vulnerability Reporting 并通过私密报告渠道提供最小复现。

发现凭据泄露时先撤销或轮换，再处理 Git 历史；只删除当前文件不足以清除已发布历史。自动扫描只能降低风险，不能证明不存在所有类型的秘密。
