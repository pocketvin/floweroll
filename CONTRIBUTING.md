# 参与开发

先阅读 `AGENTS.md`、`docs/DEVELOPMENT.md` 和改动相关的当前文档。保持 Runtime、原生执行、观察模式与展示层的职责边界。

提交前运行仓库检查与对应 Host / XCTest 回归。修复应包含重现或回归测试；原生平台限制须写清，不能用模拟结果冒充真机结果。

不提交 API Key、用户任务数据、Prompt 捕获、真实联系人/日程、设备标识、本地签名或构建产物。不要引入历史品牌兼容别名。使用本地配置，不写死作者机器路径。

当前许可证尚待所有者确认；公开接收外部贡献前先明确许可与贡献条款。不要对 fork PR 使用带私人凭据的自托管设备执行器。
